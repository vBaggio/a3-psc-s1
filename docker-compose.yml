services:
  postgres:
    image: postgres:16-alpine
    container_name: projectapp-db
    environment:
      POSTGRES_DB: projectapp
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_data:
